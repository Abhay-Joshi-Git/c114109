import React from 'react';

import WidgetWrapper from 'shared/widgetWrapper/WidgetWrapper';

const RecentEmails = () => (
  <WidgetWrapper headerOptions={{ title: 'Recent Emails' }} />
);

export default RecentEmails;
